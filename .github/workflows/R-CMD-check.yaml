---
name: R-CMD-check
'on':
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  R-CMD-check:
    runs-on: ubuntu-latest
    env:
      # This allows installation of private github repos
      GITHUB_PAT: ${{ secrets.PAT }}
    steps:
      - uses: actions/checkout@v2
      - uses: r-lib/actions/setup-r@v2
      - name: Install private packages
        run: install.packages('remotes'); remotes::install_github('uclahs-cds/private-R-BoutrosLab-statistics-general')
        shell: Rscript {0}
